const permissionButton = document.getElementById('js-permission-button');
const recordButton = document.getElementById('js-record-button');
const stopButton = document.getElementById('js-stop-button');
const playbackButton = document.getElementById('js-playback-button');
const resultButton = document.getElementById('js-result-button');
const retakeButton = document.getElementById('js-retake-button');
const exampleButton = document.getElementById('js-example-button');
const downloadLink = document.getElementById('js-download-link');
const exampleVocalLink = document.getElementById('js-example-vocal-link');
const audioInstruments = document.getElementById('js-audio-instruments');
const audioOrigin = document.getElementById('js-audio-origin');
const mixLink = document.getElementById('js-mix-link');
const mainArea = document.getElementById('js-main-area');
const canvas = document.getElementById('js-canvas');
const message = document.getElementById('js-message');
const audioPlayer = document.getElementById('js-audio-player');
const spinner = document.getElementById('js-spinner');

export default {
  permissionButton,
  recordButton,
  stopButton,
  playbackButton,
  resultButton,
  retakeButton,
  exampleButton,
  downloadLink,
  exampleVocalLink,
  audioInstruments,
  audioOrigin,
  mixLink,
  mainArea,
  canvas,
  message,
  audioPlayer,
  spinner,
}
